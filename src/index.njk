---
bodyClass: "home"
backLink: '/'
backLinkText: ''
footerLinks: false
---
{% from './_includes/article-block.njk' import articleBlock %}

{% renderTemplate 'webc', {site: site, recentListening: recentListening, social: social, caseStudies: collections['case study'], collections:collections} %}
<home-hero :@recent-listening="recentListening" :@site="site"></home-hero>
<home-letter></home-letter>
<home-about :@social="social"></home-about>
<section class="home-work themed dark">
    <h2 class="home-work__title">Selected works</h2>
    <ul class="case-study-rows" role="list">
        <case-study-row webc:for="study of sortedByPublishDate(caseStudies)" webc:if="study.data.isFeatured" :@case-study="study.data"></case-study-row> 
    </ul>
    <a class="home-work__link" href="/work">View all case studies →</a>
</section>
{% endrenderTemplate %}
<section class="home-articles">
    <h2 class="home-articles__title">Articles</h2>
    <ul class="articles-list" role="list">
        {% for article in collections['article'] | sortedByPublishDate %}
        {% if loop.index0 < 6 %}
                <li>
                     {{ articleBlock(article) }}
                </li>
            {% endif %}
        {% endfor %}
    </ul>
    <a class="home-articles__link" href="/writing">View all articles →</a>
</section>
<img class="footer-border" src="/img/footer-border.png" alt="">

{# <script src="/js/marquees.js" type="module" data-taxi-reload></script> #}