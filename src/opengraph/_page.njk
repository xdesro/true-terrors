---
pagination:
  data: "collections.page"
  size: 1
  alias: "currentPage"
layout: "layouts/og.njk"
permalink: "/opengraph/{{ currentPage.url }}/index.html"
---
<header class="header">
    <p class="intro">The personal site and portfolio of creative web developer and designer <span class="accent">Henry (from online)</span></p>
</header>
<div class="container container--wide container--bottom">
    <div class="row">
        <h1 class="page-title">{{ currentPage.data.title }}</h1>
        <div class="barb-bar"></div>
    </div>
</div>
{# <div class="container">
  <p class="intro">A <span class="accent">{{ article.data.category }}</span> from the precarious desk of <span class="accent">Henry (From Online)</span></p>
  <h1>{{ article.data.title  }}</h1>
</div>
<div class="tags">{%- for topic in article.data.topics -%}{{ topic }}{% if not loop.last %} | {% endif %}{%- endfor -%}</div>
<div class="background-art">
  {% include 'graphics/crown.svg' %}
</div> #}
